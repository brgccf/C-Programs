#include <stdio.h>
#include <stdlib.h>

int n;

void processo(int n)
{
	int i, j;
	//iterar valores até o n escolhido
	//obs> o primeiro valor do prox numero é proporcional a (n-1)
}

int main()
{
	int n;
	scanf("%d", &n);
	int loop = 3;
	while(loop)
	{
		processo(n);
		scanf("%d", &n);
	}

	return 0;
}
