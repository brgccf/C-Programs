#include <bits/stdc++.h>

int cases, a, b, c, d;
int main(int argc, char const *argv[])
{
	scanf("%d", &cases);
	while(cases)
	{
		scanf("%d", &a);
		scanf("%d", &b);
		scanf("%d", &c);
		scanf("%d", &d);
		int i;
		for (i = 0; i < count; ++i)
		{
			
		}
		--cases;
	}
	return 0;
}